<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="stylesheet" href="/stylesheets/navbar.css" />
    <link rel="stylesheet" href="/stylesheets/tabela.css" />
    <link rel="stylesheet" href="/stylesheets/busca.css" />

    <link rel="icon" href="/assets/favicon.ico" type="image/x-icon" />
    <title>GRH</title>
  </head>
  <body>
    <nav class="navbar">
      <div class="logo">
        <a href="/"
          ><img src="/assets/logoOpenPages.svg" alt="Logo OpenPages"
        /></a>
      </div>
      <ul class="nav-links">
        <li><a href="/">Home</a></li>
        <li><a href="/sugestao">Sugestão de Livro</a></li>
        <li><a href="/sobre">Sobre</a></li>
      </ul>
    </nav>
    <main>
      <h1 class="titulo">GRH</h1>
      <h2 class="subtitulo">Gestão de Recursos Humanos</h2>
      <div class="container-busca">
        <input
          type="text"
          class="campo-busca"
          placeholder="Pesquisar livros..."
        />
      </div>
      <div class="tabela-wrapper">
        <table class="tabela">
          <thead>
            <tr>
              <th>Livro</th>
              <th>Autor</th>
              <th>Editora</th>
              <th>Edição</th>
              <th>Disponibilidade</th>
            </tr>
          </thead>
          <tbody>
            <% if (livros && livros.length > 0) { %> <%
            livros.forEach(function(livro) { %>
            <tr>
              <td><%= livro.titulo %></td>
              <td><%= livro.autor %></td>
              <td><%= livro.editora %></td>
              <td><%= livro.edicao %></td>
              <td
                class="<%= livro.disponibilidade ? 'disponivel' : 'indisponivel' %>"
              >
                <%= livro.disponibilidade ? 'Disponível' : 'Indisponível' %>
              </td>
            </tr>
            <% }); %> <% } else { %>
            <tr>
              <td colspan="5">Nenhum livro encontrado</td>
            </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </main>
    <script src="/javascripts/busca.js"></script>
  </body>
</html>
